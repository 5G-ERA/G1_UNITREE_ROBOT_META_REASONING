# G1 UNITREE ROBOT — Python dependencies (Python 3.10+ recommended)
#
# Install (in a virtual environment):
#   python3 -m venv ~/g1env && source ~/g1env/bin/activate
#   pip install -r requirements.txt
#
# This file = the CLIENT that talks to the robot and runs A->B navigation
# (g1_goto.py, g1_nav_v2.py, g1_metrics.py, g1_perception.py, calibrate_cam.py,
#  plot_metrics.py, plot_health.py). It is light and has no GPU dependency.
#
# The HEAVY GPU vision server (perception_server.py) is installed separately:
#   pip install -r requirements-perception.txt      # only on the Ubuntu/GPU box

# --- Core: robot link + navigation + metrics + plots ---
websocket-client
requests
numpy
matplotlib
pillow

# --- Camera calibration (calibrate_cam.py: checkerboard intrinsics) ---
opencv-python

# --- Optional: older direct-WebRTC scripts (g1_teleop.py, slam_g1_mapping.py, ...) ---
# Community Go2/G1 WebRTC driver. Not needed for the iPhone-app/USB A->B flow.
# https://github.com/legion1581/unitree_webrtc_connect
unitree_webrtc_connect

# ----------------------------------------------------------------------------
# EXTERNAL (not pip): the A->B navigation connects to the iPhone app's WebView
# over USB and needs ios-webkit-debug-proxy on port 9221:
#   macOS:           brew install ios-webkit-debug-proxy
#   Ubuntu/Debian:   sudo apt install usbmuxd libimobiledevice-utils ios-webkit-debug-proxy
# See docs/SETUP_UBUNTU.md for the Ubuntu link setup.
